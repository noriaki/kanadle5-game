# Redis Configuration for Local Testing
# When using devcontainer, Upstash REST Server runs on port 8079
KV_REST_API_URL=http://localhost:8079
KV_REST_API_TOKEN=local-test-token

# LINE LIFF Configuration (Test)
NEXT_PUBLIC_LIFF_ID=test-liff-id
LIFF_CHANNEL_ID=test-channel-id
LIFF_CHANNEL_SECRET=test-channel-secret

# Game Configuration
DAILY_WORD_REFRESH_TIME=00:00:00 # Japan time

# Test specific
NODE_ENV=test